# Fragment: wf-sdd-trigger (MANDATORY, always-on)

<!--
  SINGLE SOURCE of the wizard's own SDD-forcing policy + wf-preflight declaration.
  MANDATORY — always included. Replaces templates/protocols/decision-ladder/local-orchestration.md.

  SCOPE CHANGE FROM THE OLD PROTOCOL (important — read before editing):
  - gentle-ai's own native orchestrator (installed/synced per-adapter, NOT generated by this
    wizard) already owns HOW to route (Direct inline / Delegated direct) and HOW to delegate SDD
    phases per adapter (Full sub-agents / Hermes delegate_task / Solo-agent inline — see
    gentle-ai docs/agents.md). This protocol NEVER re-specifies that as a fixed, hardcoded
    mechanism. The old protocol's bug was assuming ONE universal mechanism ("delegate via
    task()/spawn_agent()/run_subagent()") for every adapter. The fix is NOT to swap in a
    different hardcoded assumption per IDE name — gentle-ai has no dedicated adapter entry for
    Devin (a fork that shares Windsurf's file paths), so its installed content for "Windsurf" can
    say "no sub-agents" even when the actual running agent (Devin) genuinely has a working
    subagent/task tool. The correct rule is: self-check your OWN real toolset in this session
    before trusting any label (yours or gentle-ai's) about whether you can delegate.
  - gentle-ai only starts SDD on `explicit_request_or_accepted_proposal` — it never infers SDD
    from file count/size/risk. This wizard's own value-add is deciding WHEN this project's rules
    require that explicit request to be made — never HOW gentle-ai then executes it.
  - This protocol decides ONE thing: does the change meet this project's criteria for explicitly
    requesting SDD? If yes → `wf-force-sdd`; if no → `wf-no-sdd`. No internal severities; gentle-ai
    handles its own routing and delegation (Direct inline / sub-agents / etc.) per adapter.
  Body follows the LLM-first structure from gentle-ai's own docs/skill-style-guide.md (Activation
  Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References). Extended
  rationale/examples live in reference/edge-cases.md, not inline, to stay within the 1000-token
  hard budget. Consumed by: Builder (B3/B5), wf-refresh (R3, mandatory).
-->

## Activation Contract

Load before implementing any non-trivial change, and before `wf-ladder`/`wf-preflight` are
declared. Decides ONE thing: does this change meet this project's own rules for **explicitly
requesting** gentle-ai's SDD? See `reference/edge-cases.md` for the full rationale and examples.

## Hard Rules

- Never decide or describe HOW gentle-ai routes, delegates, or executes SDD phases — that is
  gentle-ai's native orchestrator's exclusive authority, already installed per adapter. If in
  doubt about delegation mechanics, defer entirely to gentle-ai's own installed content for this
  IDE; never invent or hardcode one here (see `reference/edge-cases.md` for the Windsurf/Devin
  capability-detection nuance).
- Never issue more than one definitive `wf-preflight` per turn; never reclassify mid-response.
- Always ask the user for confirmation of the proposed route — they may choose to override the
  gate's output (e.g., force SDD even if the gate suggested no-SDD, or skip SDD even if the gate
  suggested forcing it).

## Decision Gates

| Condition | Outcome |
|---|---|
| SIMPLE/STRAIGHTFORWARD change: typo, dependency patch, color/spacing, mechanical refactor, simple bug fix (cause & solution clear), or small new feature that is direct (max 3 files, no new abstractions, behavior is obvious from requirements) | `wf-no-sdd` |
| Any change NOT matching the above criteria (default fallback) | `wf-force-sdd` |

## Execution Steps

1. Read enough to know if the change alters existing contracts or introduces new abstractions
   (this exploration does not count as "touching the workspace").
2. Apply the Decision Gates table above, once, to a single definitive outcome.
3. Emit `wf-preflight` (Output Contract) with that outcome.
4. Always ask the user for confirmation: present the proposed route and ask if they want to:
   - Confirm it (if gate said `wf-force-sdd`: proceed to step 5; if gate said `wf-no-sdd`: proceed
     to step 6 below)
   - Override it (if gate said `wf-force-sdd`, ask if they prefer `wf-no-sdd`; if gate said
     `wf-no-sdd`, ask if they prefer `wf-force-sdd`). Loop back to step 3 with the new outcome if
     they choose to override.
5. If confirmed as `wf-force-sdd`: invoke `sdd-new <feature or fix description>` (foreground, blocking)
   to execute gentle-ai's full SDD pipeline. This satisfies gentle-ai's own
   `explicit_request_or_accepted_proposal` gate. 
   - **Important**: `sdd-new` is NOT a bash terminal command. It is a natural-language skill invocation that YOU (the agent) execute directly in this conversation, not in a shell.
   - **Slash fallback**: if your adapter only supports native slash syntax to trigger skills, invoke `/sdd-new <feature or fix description>` instead — gentle-ai recognizes it as a skill invocation. Default to the slash-less form.
   - Never describe the delegation mechanism — gentle-ai's native orchestrator handles that.
6. If confirmed as `wf-no-sdd`: proceed to implement directly (or to `wf-tdd` if that feature is
   active, per the `wf-orchestrator` sequencing).

## Output Contract

`wf-preflight` (once per task, after step 3, awaiting confirmation in step 4):

```
🔍 WF-SDD-TRIGGER
  - Criterion applied: [which condition matched, or "none"]
  - Proposal: [with SDD / without SDD]
  - Confirm? [yes / switch to the other path]
```

After user confirmation (step 4), no additional checklist is required before proceeding.

## References

- `reference/edge-cases.md` — full rationale, worked examples, and the Windsurf/Devin
  capability-detection nuance (why IDE-name labels alone are not trustworthy for delegation).
- `wf-ladder` — anti-over-engineering discipline, applied before this protocol.
- `wf-orchestrator` — single entry point that sequences this protocol with `wf-ladder`/`wf-tdd`.
- `wf-tdd` — TDD ritual referenced by the PRECHECK.
